Hi <%= @user.first_name.presence || @user.name.presence || "there" %>,

Your confirmation code is:

<%= @code %>

This code expires in 10 minutes.

If you didn’t request this, you can ignore this email.
